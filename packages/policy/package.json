{
  "name": "@mcp-sentinel/policy",
  "version": "1.0.0",
  "description": "Policy & decision engine — rules, waivers, decision logging",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "node --test dist/**/*.test.js"
  },
  "dependencies": {
    "@mcp-sentinel/core": "*"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0"
  }
}
